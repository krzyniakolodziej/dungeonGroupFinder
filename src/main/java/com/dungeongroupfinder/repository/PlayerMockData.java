package com.dungeongroupfinder.repository;

/*import com.dungeongroupfinder.entities.Player;
import org.springframework.beans.factory.annotation.Autowired;

import java.util.ArrayList;
import java.util.List;

public class PlayerMockData {

    @Autowired
    private List<Player> playerList;

    private static PlayerMockData instance = null;

    public static PlayerMockData getInstance(){
        if (instance == null) {
            instance = new PlayerMockData();
        }
        return instance;
    }

    public PlayerMockData() {
        playerList = new ArrayList<>();
        playerList.add(new Player("adam", 1));
    }
}
*/